
import { vegetableType } from 'gardenplanner/helpers/vegetable-type';
import { module, test } from 'qunit';

module('Unit | Helper | vegetable type');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = vegetableType([42]);
  assert.ok(result);
});

